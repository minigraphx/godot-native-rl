# Training + verification env (.venv-train). Used by scripts/train_*.sh and the parity/verify scripts.
#
# Deliberately UNPINNED — the exact versions live in .github/ci-constraints.txt and are applied via
# `pip install -c .github/ci-constraints.txt -r requirements-train.txt` by BOTH CI and
# scripts/setup_training.sh. The train stack has a real version tension (godot-rl 0.8.2 caps
# stable-baselines3<=2.4.0 -> numpy<2.0, but the onnx/ml_dtypes chain wants numpy>=2), so installing
# this file WITHOUT those constraints backtracks into a broken ml_dtypes sdist and crashes.
# See docs/dev/gotchas.md.
godot-rl
onnxruntime
ncnn
onnxscript
pettingzoo==1.26.1
