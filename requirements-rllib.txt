# Ray/RLlib training backend (new API stack) — OPTIONAL ray add-on for .venv-train (issue #126).
# Since #126, the RLlib backend shares .venv-train rather than a separate .venv-rllib: that venv's
# base stack (requirements-train.txt) is now SB3 2.8.0 + gymnasium 1.2.2 + numpy>=2, which matches
# ray[rllib]'s gymnasium==1.2.2 pin, so ray installs cleanly on top. setup_training.sh installs this
# into .venv-train; CI omits it (keeps the cached venv small) and the RLlib smoke skips when ray is
# absent. torch/numpy are already provided by the base stack (kept here as explicit ray runtime deps).
# See docs/superpowers/specs/2026-06-09-rllib-backend-design.md
ray[rllib]==2.55.*
torch==2.12.0
numpy
