## Trained demonstration — promotions gate on reward ✅

Real run completed (the deferred evidence from the PR description): `SCENE=res://examples/chase_the_target/chase_the_target_train_curriculum.tscn TIMESTEPS=120000 ./scripts/train_chase.sh`, exit 0. (Run uses the `SCENE`-override fix from bc537f4 — previously `train_chase.sh` ignored `SCENE=`, which this run would have silently fallen victim to.)

### Promotion timeline (both gates = `mean_reward ≥ 8.0` over a 20-episode window)

| Event | ~timesteps | `ep_rew_mean` at promotion |
|---|---|---|
| `Curriculum: promoted to stage 1 "mid"` | ~2.5k | **8.27** (gate: 8.0) |
| `Curriculum: promoted to stage 2 "hard"` | ~4.5k | **9.23** (gate: 8.0) |
| …trains on "hard" for the remaining ~115k steps… | 120k | final **11.4** |

The promotion-time rewards sitting just above the 8.0 threshold show the gate doing exactly its job — the controller waited for the 20-episode window to clear the bar, then advanced. Final reward on the hardest stage (full 1000×600 arena, touch_radius 40) exceeds what the easy stage was earning at promotion, i.e. the policy genuinely progressed through the staged difficulty rather than coasting.

Early stages clear quickly by design (a big touch radius in a small arena is easy) — for a slower arc, raise the JSON thresholds/windows in `chase_curriculum.json`; the mechanism is the deliverable here.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
