extends Node
# Test stub: a Node (like real AGENT-group agents) exposing a `policy_name` property
# like the real controllers do, so it can populate NcnnSync.agents_training (Array[Node]).

var policy_name: String = "shared_policy"
